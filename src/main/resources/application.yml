# GENERAL
server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost/db_tasks?useSSL=false
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update